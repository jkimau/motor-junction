import React from 'react';
import styled from 'styled-components';

import { GLOBAL_GOLD } from '../colors';

const Footer = styled.div`
    border-top: 1px solid ${GLOBAL_GOLD};
`;

export default () => (
    <Footer>
        Footer
    </Footer>
)